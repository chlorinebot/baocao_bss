import { jsx as _jsx } from "react/jsx-runtime";
import { render } from 'ink';
import BigText from 'ink-big-text';
import Gradient from 'ink-gradient';
const Logo = ({ text, colors }) => {
    // ink-gradient with custom colors
    if (colors.length > 0) {
        return (_jsx(Gradient, { colors: colors, children: _jsx(BigText, { text: text, font: "block" }) }));
    }
    // Default gradient
    return (_jsx(Gradient, { name: "rainbow", children: _jsx(BigText, { text: text, font: "block" }) }));
};
export function renderInkLogo(text, palette) {
    return new Promise((resolve) => {
        const { unmount } = render(_jsx(Logo, { text: text, colors: palette }));
        // Automatically unmount after rendering to allow process to exit
        setTimeout(() => {
            unmount();
            resolve();
        }, 100);
    });
}
//# sourceMappingURL=InkRenderer.js.map